export class Contact {
  /**
   * @param {string} id - The unique identifier for the contact.
   * @param {string} postcode - The postcode for the contact.
   */
  constructor(id, postcode) {
    this.id = id
    this.postcode = postcode
  }
}
