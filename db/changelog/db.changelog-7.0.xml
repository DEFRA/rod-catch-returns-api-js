<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
                   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                   xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.4.xsd">

    <!--
        update all rivers called Esk
    -->
    <changeSet id="update all rivers called Esk" author="Defra">
        <sql>
            <![CDATA[
                UPDATE rcr_river set name = 'Border Esk', last_modified = now(), version = now() WHERE name = 'Esk (Border)';
                UPDATE rcr_river set name = 'Cumbrian Esk (Ravenglass)', last_modified = now(), version = now() WHERE name = 'Esk (Cumbria)';
                UPDATE rcr_river set name = 'Yorkshire Esk (Whitby)', last_modified = now(), version = now() WHERE name = 'Esk (Yorkshire)';
            ]]>
        </sql>
        <rollback>
            <sql>
                <![CDATA[
                    UPDATE rcr_river set name = 'Esk (Border)', last_modified = now(), version = now() WHERE name = 'Border Esk';
                    UPDATE rcr_river set name = 'Esk (Cumbria)', last_modified = now(), version = now() WHERE name = 'Cumbrian Esk (Ravenglass)';
                    UPDATE rcr_river set name = 'Esk (Yorkshire)', last_modified = now(), version = now() WHERE name = 'Yorkshire Esk (Whitby)';
                ]]>
            </sql>
        </rollback>
    </changeSet>

</databaseChangeLog>
